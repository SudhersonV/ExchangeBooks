@model IEnumerable<string>
@{
    ViewData["Title"] = "Refresh Token";
}

<div class="text-center">
    <h2 class="display-4">You are seeing the refreshed tokens</h2>

    @if (Model.Any())
    {
        foreach (var m in Model)
        {
            <p>@m</p>
        }
    }
</div>
